__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.env
.env.*
!.env.example
!.env.production.example

# Database
*.db
*.sqlite
*.sqlite3
/var/
/data/local/

# Editor
.vscode/
.idea/
*.swp
.DS_Store

# Tests / tooling
.pytest_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/
.mypy_cache/
.ruff_cache/

# Build
build/
dist/

# Sb_UI_11.1 — Visual baseline outputs (never committed).
# Also covered by existing rules: `.env.*` (line 8) and `/var/` (line 16)
# already ignore `.env.baseline` and `var/visual-baseline/` respectively.
# Explicit additions for clarity + non-covered paths:
baseline/
test-results/visual-baseline/
